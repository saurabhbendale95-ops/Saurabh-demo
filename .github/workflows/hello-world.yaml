name: Hello world demo

on:  
  push:
    branches:
      - main

jobs:
    say-hello:
      runs-on: ubuntu-latest
      steps:
        - name: checkout code
          uses: actions/checkout@v3


        - name: Print Hello
          run: echo " Hello, GitHub Actions!"
          
        
  
        
